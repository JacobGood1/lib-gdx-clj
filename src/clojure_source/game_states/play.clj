(ns game-states.play
  (:import (com.badlogic.gdx.graphics.g2d BitmapFont))
  (:require [game-states.macro-util :as util])
  (:use [language-extensions.core]))

(defn update
  [dt]
  )

(defn render
  [])



;include this when making game
;exclude this when shipping game
;TODO remove
(util/make-interactive)